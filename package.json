{
  "name": "pixeltrace",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "yarn workspace web dev",
    "dev:api": "yarn workspace api start:dev",
    "build": "npm install -g yarn && yarn install && yarn workspaces run build",
    "build:api": "npm install -g yarn && yarn install && yarn workspace api prisma:generate && yarn workspace api build",
    "build:web": "npm install -g yarn && yarn install && yarn workspace web build",
    "db:generate": "yarn workspace api prisma generate",
    "db:migrate": "yarn workspace api prisma migrate dev",
    "db:studio": "yarn workspace api prisma studio"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
